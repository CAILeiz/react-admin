# react redux webpack antd

# 接口包含
1. 请求 请求url 请求type 请求param
2. 响应 成功 失败

# 目录结构
src 
    api             请求相关
    assets          公共资源
    components      非路由组件
    config          配置
    pages           路由组件
    utils           工具模块
    App.jsx         应用根组件
    index.js        入口js